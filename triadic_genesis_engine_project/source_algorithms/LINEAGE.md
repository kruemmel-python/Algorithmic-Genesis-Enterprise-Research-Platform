# Source Algorithm Lineage

This directory contains the original exported Python algorithms and manifests from the uploaded `webui_runs.zip`.

They are not modified. The new project imports their kernels conceptually and re-implements the exact mathematical expressions in `src/triadic_genesis_engine/core.py` to avoid dynamic-path coupling.
